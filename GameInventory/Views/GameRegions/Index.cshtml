@model IEnumerable<GameInventory.GameRegion>

@{
    ViewBag.Title = "Regions";
}

<link rel="stylesheet" type="text/css" href="~/Content/bootstrap.min.css" />
<link rel="stylesheet" type="text/css" href="~/Content/datatables.min.css" />
<script type="text/javascript" src="~/Scripts/jquery-1.10.2.min.js"></script>
<script type="text/javascript" src="~/Scripts/datatables.min.js"></script>
<h2>Regions</h2>
<h4>Total: @Model.Count()</h4>

@if (User.Identity.IsAuthenticated)
{
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
}
<table id="RegionTable" class="table table-striped">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.GameRegionName)
            </th>
            <th>
                Actions
            </th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.GameRegionName)
                </td>
                <td>
                    @if(User.Identity.IsAuthenticated)
                    { 
                        @Html.ActionLink("Edit", "Edit", new { id = item.GameRegionId }) @:|
                        @Html.ActionLink("Details", "Details", new { id = item.GameRegionId }) @:|
                        @Html.ActionLink("Delete", "Delete", new { id = item.GameRegionId })
                    } else {
                        @Html.ActionLink("Details", "Details", new { id = item.GameRegionId })
                    }
                </td>
            </tr>
        }
    </tbody>
</table>

<script type="text/javascript">
    $("#RegionTable").DataTable();
</script>
