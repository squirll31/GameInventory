@model GameInventory.GameRegion

@{
    ViewBag.Title = "Details";
}

<h2>Details</h2>

<div>
    <h4>GameRegion</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.GameRegionName)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.GameRegionName)
        </dd>

    </dl>
</div>
<p>
    @if (User.Identity.IsAuthenticated)
    {
        @Html.ActionLink("Edit", "Edit", new { id = Model.GameRegionId }) @:|
        @Html.ActionLink("Back to List", "Index")
    } else {
        @Html.ActionLink("Back to List", "Index")
    }
</p>
