@model GameInventory.CompanyRelationshipType

@{
    ViewBag.Title = "Details";
}

<h2>Details</h2>

<div>
    <h4>CompanyRelationshipType</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.CompanyRelationshipTypeName)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.CompanyRelationshipTypeName)
        </dd>

    </dl>
</div>
<p>
    @if (User.Identity.IsAuthenticated)
    {
        @Html.ActionLink("Edit", "Edit", new { id = Model.CompanyRelationshipTypeId }) @:|
        @Html.ActionLink("Back to List", "Index")
    } else {
        @Html.ActionLink("Back to List", "Index")
    }
</p>
